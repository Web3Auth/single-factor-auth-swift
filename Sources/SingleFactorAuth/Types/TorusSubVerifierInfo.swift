class TorusSubVerifierInfo {
    let verifier: String
    let idToken: String
    
    init(verifier: String, idToken: String) {
        self.verifier = verifier
        self.idToken = idToken
    }
}
